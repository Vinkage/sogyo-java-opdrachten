package nl.sogyo.javaopdrachten.exceptionalusers;

import java.util.regex.Pattern;

public interface DatabaseUser {
    static Boolean validatePassword(String password) {
        Pattern validPasswordPattern = Pattern.compile();
    }
}
